import { Switch } from 'antd';
import styled from 'styled-components';

export const ControlStyled = styled(Switch)`
  /* &.ant-switch .ant-switch-inner .ant-switch-inner-unchecked {
    margin-top: -23px;
  } */
`;
